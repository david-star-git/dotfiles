# Window 0 (default) → just a single pane
rename-window "main"

# Window 1 layout
new-window -n "work1"
split-window -h             # split into left and right
select-pane -t 1            # focus right side
split-window -v             # split right into top+bottom
select-layout even-horizontal

# Window 2 layout
new-window -n "work2"
split-window -h
select-pane -t 1
split-window -v
select-layout even-horizontal

# Go back to window 0 by default
select-window -t 0

