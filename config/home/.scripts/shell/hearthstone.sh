# hearthstone launch Hearthstone on Linux
# kept in shell/ so it's sourced into the current shell (cd would be lost otherwise)
cd ~/Documents/git/hearthstone-linux/hearthstone || return
./Bin/Hearthstone.x86_64
